%h1 Listing users

%table
  %thead
    %tr
      %th First name
      %th Last name
      %th Description
      %th Position
      %th Slack nick
      %th Eaten doughnuts
      %th Awaiting doughnuts
      %th Delivered doughnuts
      %th Bloopers count
      %th Team
      %th
      %th
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.first_name
        %td= user.last_name
        %td= user.description
        %td= user.position
        %td= user.slack_nick
        %td= user.eaten_doughnuts
        %td= user.awaiting_doughnuts
        %td= user.delivered_doughnuts
        %td= user.bloopers_count
        %td= user.team_id
        %td= link_to 'Show', user
        %td= link_to 'Edit', edit_user_path(user)
        %td= link_to 'Destroy', user_path(user.slack_nick), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New User', new_user_path
